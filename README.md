# Driver Timeline Backend

## Setup Instructions
This is build using express server.


Follow the steps below to run the backend server locally.

Sqlite has been used as database.

---

### Step 1: Clone the repository

```bash
git clone 
cd driver-timeline-backend
npm i
nodemon server.js
